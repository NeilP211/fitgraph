/**
 * RunwayHero removed at Neil's request (2026-05-23). The cream catwalk hero with
 * floating garment thumbnails was cut from the homepage because the big empty
 * stage read as awkward. Kept as a no-op default export so the homepage import
 * keeps resolving; the import and usage can be dropped from app/page.tsx
 * whenever that file is next edited.
 */
export default function RunwayHero() {
  return null;
}
